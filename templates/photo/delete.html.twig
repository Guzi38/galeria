{% include 'default/_form.html.twig' with {
    page_title: 'Usuń zdjęcie'|trans({'%id%': photo.id|default('')}),
    form: form,
    submit_label: 'usuń'|trans,
    back_to_list_path: 'photo_index',
} %}