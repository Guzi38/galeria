{% include 'default/_form.html.twig' with {
    page_title: 'Usuń Galerie'|trans({'%id%': gallery.id|default('')}),
    form: form,
    submit_label: 'usuń'|trans,
    back_to_list_path: 'gallery_index',
} %}
